{
  "id": "pseudoco-assistant-toolguard",
  "name": "PseudoCo Assistant Tool Guard",
  "description": "Governed before_tool_call: PseudoCo Assistant tool policy + NemoClaw policy layer + Cisco AI Defense inspect every agent tool call before execution; after_tool_call reports sandbox policy denials",
  "version": "1.1.0",
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": []
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "guardUrl": {
        "type": "string",
        "description": "Base URL of the PseudoCo Assistant app as seen from the gateway/sandbox, e.g. http://host.containers.internal:8001 (podman) or http://<host ip>:8001 (NemoClaw)"
      },
      "accessKey": {
        "type": "string",
        "description": "PseudoCo Assistant ACCESS_KEY (sent as HTTP Basic password)"
      },
      "failOpen": {
        "type": "boolean",
        "description": "true = allow tool calls when the guard is unreachable; false (default) = fail closed and block",
        "default": false
      },
      "timeoutMs": {
        "type": "number",
        "description": "HTTP timeout for the guard call in milliseconds (keep under the hook timeoutMs of 10000)",
        "default": 8000
      },
      "agentSurface": {
        "type": "string",
        "description": "Attribution label for governance events: openclaw (default) or nemoclaw when running inside the NemoClaw/OpenShell sandbox",
        "default": "openclaw"
      }
    },
    "required": ["guardUrl"]
  }
}
